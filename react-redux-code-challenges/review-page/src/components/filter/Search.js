import React from 'react';
import Input from '../form-elements/Input';

const Search = () => {
    return (
        <div>
            <Input 
                placeholder="Search"
            />
        </div>
    );
};

export default Search;